{
    "scripts": {
        "build": "lerna run build",
        "prepare": "lerna bootstrap",
        "test": "lerna run test",
        "start": "lerna run start",
        "docs": "lerna run docs",
        "lint": "lerna run lint"
    },
    "workspaces": [
        "packages/*"
    ],
    "private": true,
    "jest": {
        "projects": [
            "packages/*"
        ],
        "preset": "./jest-defaults.json"
    },
    "devDependencies": {
        "jest": "^25.1.0",
        "lerna": "^3.2.1"
    }
}
